name: Pull Request Checks
on:
  pull_request:
    branches:
      - main
jobs:
  build:
    runs-on: ubuntu-latest
    name:
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Install dependencies
        run: npm install

      - name: Run tests
        run: npm test

      # - name: Run linter
      #   run: npm run lint

      - name: Run build
        run: npm run build
